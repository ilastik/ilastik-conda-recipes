package:
  name: ilastiktools
  version: "0.1"

source:
  git_url: https://github.com/ilastik/ilastiktools
  git_tag: 6cdd7d2663b2f3ab0a2428c5b1d7c0bf94801027

build:
  number: 5

  #
  # You must build this recipe using 'conda build --numpy=X.Y'
  #
  string: np{{np}}py{{py}}_{{PKG_BUILDNUM}}_g{{GIT_FULL_HASH[:7]}}

  detect_binary_files_with_prefix: true

requirements:
  build:
    - gcc 4.8.5 # [linux]
    - gcc 4.8.5 # [osx]
    - python 2.7*
    - numpy {{NPY_VER}}*
    - vigra 1.10*

  run:
    - libgcc
    - python {{PY_VER}}*
    - numpy  {{NPY_VER}}*
    - vigra 1.10*

test:
  imports:
    - ilastiktools

about:
  home: http://ilastik.org
  license: GPLv3
  summary: Various compiled utilities for the ilastik project
